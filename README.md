# 📷 PyQt6 OCR Scanner

Instant OCR Scanner using PyQt6, OpenCV and Tesseract.

## Features

- Live webcam preview
- Camera selection
- Capture & OCR extraction
- Stores up to 5 lines per scan
- Append to CSV export

## Requirements

Install dependencies:

```bash
pip install -r requirements.txt
 